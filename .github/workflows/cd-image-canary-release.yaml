# Copyright 2023 The Atlas Operator Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CD - Image Canary Release
on:
  push:
    branches:
      - dev
    paths:
      - 'Dockerfile'
      - 'Makefile'
      - 'cmd/**'
      - 'api/**'
      - 'internal/**'
      - 'charts/**'
      - '.github/workflows/cd-image-canary-release.yaml'
jobs:
  push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@885d1462b80bc1c1c7f0b00334ad271f09369c55 # v2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@2b82ce82d56a2a04d2637cd93a637ae1b359c0a7 # v2
      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@818d4b7b91585d195f67373fd9cb0332e31a7175 # v4
        with:
          images: ghcr.io/${{ github.repository_owner }}/atlas-operator
          tags: |
            type=raw,value=dev
            type=schedule
            type=sha
            type=ref,event=branch
            type=semver,pattern={{version}}
          labels: |
            io.ariga.atlas.version=dev-01c1774a6484596092eed387d7cdda9355e5a896
            io.ariga.atlas.channel=mutable-test
            org.opencontainers.image.title=atlas-operator
            org.opencontainers.image.description=Atlas Operator
            org.opencontainers.image.url=https://atlasgo.io
            org.opencontainers.image.vendor=Ariga
            org.opencontainers.image.source=https://github.com/esafak/atlas-operator/blob/dev/Dockerfile
      - name: Build and push
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: .
          build-args: |
            ATLAS_REPOSITORY=esafak/atlas
            ATLAS_RELEASE=dev
            ATLAS_COMMIT=01c1774a6484596092eed387d7cdda9355e5a896
            ATLAS_ASSET_BASE=https://github.com/esafak/atlas/releases/download/dev
            ATLAS_AMD64_SHA256=c586d2c3d2014020a83c820637e9f5e9855d69835564b454a33ca7f6577aaa5c
            ATLAS_ARM64_SHA256=b860900e2f9640e8305882d0b5e812af7430a91332f48ff70a054c3564f345ab
            OPERATOR_VERSION=dev-${{ github.sha }}
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: 'template'
          template: '@/contrib/sarif.tpl'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@03e4368ac7daa2bd82b3e85262f3bf87ee112f57 # v3
        with:
          sarif_file: 'trivy-results.sarif'
